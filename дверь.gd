extends MeshInstance


# Declare member variables here. Examples:
# var a = 2
# var b = "text"
#var angle = 0

# Called when the node enters the scene tree for the first time.
#func _ready():
#	$Door.rotation.y = angle # Replace with function body.


#func open_close():# Called every frame. 'delta' is the elapsed time since the previous fram
#if !angle:
	#angle = deg2rad(95)
#else:
	#angle = 0
